let follow = document.querySelector('#follow');
if(follow){
  follow.parentElement.removeChild(follow);
}

document.querySelector('.show-options').insertAdjacentHTML('afterbegin','<%= j render("users/unfollow") %>')
